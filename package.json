{
  "name": "mern-url-shortener",
  "version": "1.0.0",
  "description": "A full-stack URL shortener built with MongoDB, Express, React and Node (MERN).",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n API,WEB -c green,cyan \"npm:dev:backend\" \"npm:dev:frontend\"",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm run build:frontend && npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
